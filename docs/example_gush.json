{
  "commands": [
    {
      "command": "bundle install --path vendor/bundle",
      "cwd": "$GIT_REPO"
    },
    {
      "command": "rake assets:precompile",
      "cwd": "$GIT_REPO"
    },
    {
      "command": "cp -R public/assets/* /app/current/assets/"
      "cwd": "$GIT_REPO"
    }
  ]
}
